/** AsyncStorage keys — values kept for existing user data. */
export const STORAGE_KEYS = {
  events: '@sportyritcountddown_events_v1',
  greetings: '@sportyritcountddown_saved_greetings_v1',
  pushNotifications: 'pushNotifications_v1',
  quizBestScore: 'quizbestscore_v1',
} as const;
